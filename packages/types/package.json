{
  "name": "@sovereign/types",
  "version": "0.0.1",
  "private": true,
  "description": "Sovereign Ecosystem — Shared TypeScript types (non-DB)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
