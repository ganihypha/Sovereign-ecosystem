{
  "name": "@sovereign/integrations",
  "version": "0.1.0",
  "private": true,
  "description": "Sovereign Ecosystem — Integration contracts and scaffolds for Fonnte WA and Groq LLM (Cloudflare Workers compatible)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
