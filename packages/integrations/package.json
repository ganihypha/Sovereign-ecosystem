{
  "name": "@sovereign/integrations",
  "version": "0.0.1",
  "private": true,
  "description": "Sovereign Ecosystem — External API clients (Supabase, Fonnte, ScraperAPI, LLM)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sovereign/types": "workspace:*",
    "@supabase/supabase-js": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
