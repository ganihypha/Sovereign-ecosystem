// @sovereign/db — Placeholder
// Phase 2: DB schema, types, query helpers akan diisi di sini
// Lihat: docs/24-DATABASE-INVENTORY-AND-MIGRATION-MAP.md

// TODO Phase 2:
// - schema.ts: semua table definitions
// - types.ts: DB-level TypeScript types
// - query helpers per domain (leads, orders, customers, etc)

export const DB_VERSION = '0.0.1'
export const DB_PLACEHOLDER = true

// Placeholder export — akan diganti dengan real implementation di Phase 2
export type DbConfig = {
  url: string
  serviceRoleKey: string
  anonKey: string
}
