{
  "name": "@sovereign/db",
  "version": "0.1.0",
  "private": true,
  "description": "Sovereign Ecosystem — DB schema, types, query helpers (Supabase). Canonical database layer untuk seluruh ecosystem.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit --strict"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "keywords": [
    "sovereign",
    "supabase",
    "database",
    "schema"
  ]
}
