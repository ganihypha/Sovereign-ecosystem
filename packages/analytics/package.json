{
  "name": "@sovereign/analytics",
  "version": "0.0.1",
  "private": true,
  "description": "Sovereign Ecosystem — Metrics, tracking, KPI helpers",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sovereign/types": "workspace:*",
    "@sovereign/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
