{
  "name": "@sovereign/ui",
  "version": "0.0.1",
  "private": true,
  "description": "Sovereign Ecosystem — Shared UI components (React/HTML + Tailwind)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sovereign/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
