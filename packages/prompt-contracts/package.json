{
  "name": "@sovereign/prompt-contracts",
  "version": "0.0.1",
  "private": true,
  "description": "Sovereign Ecosystem — Agent system prompts + output schemas",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sovereign/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
