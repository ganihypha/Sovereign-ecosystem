{
  "name": "@sovereign/auth",
  "version": "0.0.1",
  "private": true,
  "description": "Sovereign Ecosystem — JWT validation, role checking, auth middleware",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
