{
  "name": "@sovereign/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Sovereign Ecosystem — JWT foundation, role guards, Hono middleware (Cloudflare Workers compatible)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
